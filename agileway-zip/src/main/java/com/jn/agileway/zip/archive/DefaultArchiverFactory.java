package com.jn.agileway.zip.archive;

import java.io.OutputStream;

public class DefaultArchiverFactory implements ArchiverFactory {
    @Override
    public Archiver get(String format, OutputStream outputStream) {
        return null;
    }
}
